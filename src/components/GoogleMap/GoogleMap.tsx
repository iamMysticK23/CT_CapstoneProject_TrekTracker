import React from 'react';

// internal imports
import { NavBar } from '../sharedComponents';


export const GoogleMap = () => {
    return (
        <div>
            <NavBar />
            <h1>Google Map</h1>
        </div>
    )
}